<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.WPF"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Class="PL.WPF.SimulateOneStationWindow"
        mc:Ignorable="d"
        Title="SimulateOneStationWindow" Height="450" Width="800">
    <Grid>
        <Grid x:Name="gridOneStation">
            <TextBlock x:Name="timerTextBlock"/>
        </Grid>
        <Grid >
            <DataGrid x:Name="lineTimingDataGrid" Margin="181,26,440.6,160"  Background="#FF9CB7F4">

                <ListView x:Name="lineTiminglv">
                    <ListView.ItemTemplate>
                        <DataTemplate/>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock Text="lines that pass through the station" Height="34" Width="269"/>
            </DataGrid>

            <materialDesign:TimePicker IsDropDownOpen="True" AllowDrop="True" HorizontalAlignment="Left" Margin="555,26,0,0" VerticalAlignment="Top" Width="139" Height="48"/>
            
           
 
        </Grid>
    </Grid>
</Window>
